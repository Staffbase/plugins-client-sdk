// let expect = require('chai').expect;
/*

window = {};
window.parent = {};
window.addEventListener = function(message, receiveMessage) {};
window.parent.postMessage = function(message, origin) {};

const deviceCanDownload = require('..').deviceCanDownload;
console.log('devccandl',deviceCanDownload);
describe('deviceCanDownload', function() {

	it('should be true allways', async () => {
		const downloadFlag = await deviceCanDownload()
	    expect(downloadFlag).to.equal(true);

	});
});
*/
